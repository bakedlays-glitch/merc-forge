"""MapForge subsystem: sector editing primitives + generators.

The package's main public surface is `generators` — the registry of
built-in map generators that MapForge's UI surfaces in its Generators
panel and console (`:gen <name>`). See `generators/README.md` for the
authoring contract.
"""
